/**
 * Nucleus Firebase Config
 * =======================
 * INSTRUCTIONS:
 * 1. Go to https://console.firebase.google.com
 * 2. Click "Add project" → name it "nucleus-cms" → Continue → Create Project
 * 3. Click the "</>" (Web) icon → Register app → you'll see firebaseConfig
 * 4. Copy and paste your values below (replace the empty strings)
 * 5. In Firebase console: go to Firestore Database → Create → Start in Test Mode
 * 6. In Firebase console: go to Storage → Get Started → Start in Test Mode
 * 7. Save this file, then run: git add . && git commit -m "Add Firebase" && git push
 * 8. Done! Admin changes will now appear on the live site for all visitors.
 */

window.NUCLEUS_FIREBASE_CONFIG = {
  apiKey:            "",
  authDomain:        "",
  projectId:         "",
  storageBucket:     "",
  messagingSenderId: "",
  appId:             ""
};
